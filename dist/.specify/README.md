# .specify - the spec engine's shared runtime (generated)

Scripts and ephemeral-artifact templates shared by the `speckit-*` skills in
`.claude/skills/`. Based on github/spec-kit v0.13.2 (MIT - see LICENSE), patched by
repository-standards (capability paths, the clarify gate, the standard's own spec and
constitution templates). Generated from `standard/spec-kit/` - do not hand-edit.
