---
audience: technical
type: added
---
- `decision-records/adr/ADR-005` - proposed: the align/update/self-verify engine reads a versioned, declarative **manifest** (files/sections/guards/decisions + adapt rules) instead of prose only - drift becomes a number, an update becomes a manifest delta. Ships a worked, illustrative manifest (`ADR-005.manifest.example.json`). Not wired in until Accepted (ENG-2).
