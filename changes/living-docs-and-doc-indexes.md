---
audience: both
type: added
headline: Documentation is living - update in place; index the docs when they multiply
---
- `docs/taxonomy.md` - two new sections. **Living documents - updated in place**: specs, docs, and records change with the product by editing the same file; the current version is the truth, git is the history; significant reversals get one in-doc line on what changed and why; accepted records are never rewritten into a different decision - they are superseded (status flip + link). **When documents multiply - index them**: a per-directory `README.md` index (one line per doc), pointed at from `AGENTS.md`, updated in the same PR as the docs it maps. The backlog gains the epics distilled from the 2026-07-21 notes pass: artifact lifecycle & tracker sync (LIFE-1/2, to be designed with IDEA-1) and the guided compliance loop (GUIDE-1/2), plus EXPLAIN-1 (plain-language explain mode for the PO).
