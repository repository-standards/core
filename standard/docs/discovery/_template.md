# <Topic> - discovery dossier

<!-- Copy to docs/discovery/<topic>/README.md. One folder per topic; one file per
     source beside this one, named <date>-<what-it-was>.md. Never normative: where
     this and a spec disagree, the spec has already won. -->

**Summary.** What this topic is, and - once it matures - what came out of it, linked.

**Last reconciled:** `<date> (specs/<capability> @ <commit>)`

<!-- The stamp is what makes "explain it once" mechanical. The spec skills read only
     entries newer than this, plus anything still new/open. Update it when you fold
     the dossier into a spec; never edit it by hand to silence a question. -->

## Entries

| Date | Source | State |
|---|---|---|
| `<YYYY-MM-DD>` | `<file>.md` - one line on what it was | `new` |

<!-- State moves new -> folded-into-spec | superseded-by: <record> | open -->

## Contradictions to resolve

| What disagrees | Source A | Source B |
|---|---|---|
| | | |

<!-- Raised by discovery-digest when a new entry disagrees with an earlier one.
     Each row names both sources so the disagreement can be taken to a person. -->
