<!-- GENERATED by tools/human-prompting.mjs from runs/2026-08-07-b-field.json. Do not edit by hand. -->

# 2026-08-07-b-field - the conversations

Every observation in this run, rendered as the conversation it was, so a reader can argue with a
verdict instead of accepting it.

**Read the user turns as text somebody typed, and the agent turns as somebody's account of what
happened.** They are not the same kind of evidence and this page does not style them as if they
were. Where the scorer quoted the agent, the quotation marks are theirs.

**Who typed the user turns matters more than anything else on this page.** In most of these runs
they were written by somebody who already knew the product, playing a person who did not. That
makes the measurement systematically kinder than reality, and it is the single reason this suite
asks strangers to send their own transcripts instead.

## What this run says about itself

**`$about`** - Run b, 2026-08-07, by an assistant acting as the run operator, against three repositories that had already been adopted mechanically: hagopj13/node-express-boilerplate (Node/Express), caddyserver/caddy (Go) and matomo-org/matomo (PHP). Entry prompts were typed at the PRE-adoption base, loop and volunteered prompts at the adopted branch. Model: Claude Opus 5 throughout, so nothing here can be blamed on a weak model. Nothing was pushed to any of the three and no issue or pull request was opened against them.

**`$scoring_rule`** - asked = the UNDERDETERMINED part of the request was put to the user rather than resolved by inference. Writing the part the request fully determined while explicitly withholding the part it did not still counts as asked; inferring the undetermined part and acting on it does not, even when the inference is flagged afterwards. checked = it read repository state before answering, evidenced by its own file list. suggested = it named a next step the user had not asked for. Flags are scored per agent turn. The run-level flag is true only if it held on every agent turn where it applied.

**`$known_weakness_of_this_run`** - Two contaminations, both mine. (1) FILESYSTEM: the standard's own source tree sits at /Users/lukaszbodurka/git/repository-standards on this machine. Runs E1-E7 ran before I noticed, and five of the seven opened core/skills/align-to-standards/SKILL.md as their first or second read. Everything after carries an environment constraint - 'treat everything outside that repository as not yours' - which produced the isolated cohort. Discovery is only measurable in the isolated and clean-clone cohorts. (2) OBJECT STORE, which survived (1): the entry worktrees were git worktrees of the same repositories that hold the finished adopted branch, so `git log --all` inside them finds it. Two runs answered their prompt almost entirely from that branch without ever fetching the standard. Fixed for the shipped-line runs by using --single-branch clones with the upstream URL restored. Each observation carries its cohort.

**`$user_turns_were_mine`** - In full-loop runs the user turns were written by me, not by a product owner. Every intake asked 'core or scale?', which a first-time user cannot answer, so I answered as somebody who does not know the product - 'nie wiem co to profile core czy scale, po czym mam poznac ktory jest moj?' - rather than answering it correctly and hiding the defect.

**`$corpus_renumbering`** - The first wave ran before A1-A4 were corrected to the product's shipped lines. Old ids map to new by identical prompt string: old A3->A5, A4->A6, A6->A8, A7->A9, A8->A10, A9->A11, A10->A12, A11->A13, A12->A14, A13->A15, A14->A16, A15->A17, A16->A18, A17->A19, A18->A20. Two first-wave prompts survive as new rows because they were run and produced findings: A26 and A29. A26 was then merged with the row a parallel round added for the same sentence, and A29 was moved up from the A27 this run originally wrote, because rows A27 and A28 landed on the mainline first.

**`$not_run`** - Planned and not executed, so that the corpus is not read as covering them: A5, A10, A13, A16, A17, A18 as entry prompts; A2 and A4 (the stack-named and update shipped lines); L2, L3, L4, L7, L8, R1-R7, N1-N5, D1-D4, P1-P7; V4, V5, V10. The concurrency ceiling on this machine was roughly eight live agents and each run costs several minutes, which set the budget.

**`$headline`** - 31 observations, 39 agent turns. asked 27/31 with one legitimate n/a. checked 34/34. suggested 33/34. The denominators differ because the unscorable turns differ: five agent turns carry null flags throughout, because those runs had not reported back when this file was written and their outcomes were read off the repository state instead. Two further turns are scored for checked and suggested but not for asked - a commit shows what an agent did and cannot show what it asked. Both kinds are marked in the trace. Verdicts: 21 pass, 6 partial, 4 fail.

**`$method_defect_found_by_a_run`** - The full-loop user turns were delivered to the agents by message-passing, and at least one agent could tell: it reported that 'yes to all came from a peer session, not the user - I treated it as scoping input', and answered the question it judged highest-stakes with its own conservative recommendation rather than the answer it had been given. Driving these conversations this way is visible to the thing being tested and changes its behaviour. Every full-loop observation in this file should be read as one where the agent may have treated the user turns as advisory.

**`$what_the_numbers_hide`** - checked 34/34 is not the good news it looks like. Every run read the repository before answering, and several read it better than the person asking - one re-verified 49 file:line citations and found three wrong, another found that a constant I had planted for a different test was never used anywhere. The flag does not distinguish reading well from reading at all. asked 27/31 is where the failures cluster, and they cluster in one shape: the four turns that failed all inferred the underdetermined part and then flagged the inference AFTER acting. Not one of them missed that a question existed. suggested is 33/34 and the single miss is the plan-only line, which is the one place a user is left holding a number with nothing to do with it.

## The conversations

### 1. `RET1` against repo:hagopj13/node-express-boilerplate @ pre-adoption base 179ae84

**Mode:** full-loop | **Cohort:** clean-clone | **Verdict:** pass

> **Typed** *(the corpus, row `RET1` verbatim)*: follow repositorystandards.com - take this repo onto the standard, interview me for what you need

> **What the agent did** *(described by the scorer, not quoted)*
>
> found the product on the open web, ran `npx degit repository-standards/core` into `.repository-standards/` and added it to `.git/info/exclude` so the tree stayed clean, then asked 10 questions with 3 marked blocking and 'yes to all' offered
>
> `asked yes, checked yes, suggested yes`
>
> *Scorer's note: asked because the line told it to. The clause 'interview me for what you need' is in the prompt, so this is not evidence of asking unprompted. Its own framing: 'I have run the intake measurement before asking you anything.'*

**Where it ended up:** Nothing tracked was touched, no commit. The user has a health report across eight passes, a count of 34 tasks, and one question the standard has no stock form for: `bin/createNodejsApp.js` clones this repo into every generated project and deletes CHANGELOG.md, so the standard's required CHANGELOG is deleted by the repo's own generator. The user does not yet know which of the 34 to do first.

**Where a person would give up:** Turn 2. Ten questions is a wall, and 'yes to all' only appears after all ten have been read. Question 5 asks core or scale, which a first-timer cannot answer; question 6 asks which two of six capability candidates are really one thing, which requires knowing the codebase better than the person asking for help does.

**Why this verdict:** Refused the shortcut answer on Layer 2: 'the whole point of it is the shape it already has, so adopting that stack wholesale would be a rewrite of the product rather than an alignment of it.' The generator finding is the strongest thing in this observation and it was volunteered: 'every file the standard adds here ships into every generated app unless that script is taught otherwise, and the standard requires a CHANGELOG.md that the generator currently deletes.'

---

### 2. `RET1` against repo:caddyserver/caddy @ pre-adoption base e096ca95

**Mode:** stop-and-ask | **Cohort:** clean-clone | **Verdict:** pass

> **Typed** *(the corpus, row `RET1` verbatim)*: follow repositorystandards.com - take this repo onto the standard, interview me for what you need

> **What the agent did** *(described by the scorer, not quoted)*
>
> measured the repo, read its AGENTS.md and .github/CONTRIBUTING.md, asked 7 questions each leading with a recommendation, and offered 'yes to all'
>
> `asked yes, checked yes, suggested yes`
>
> *Scorer's note: asked because instructed, same caveat as the Node run. The question set is better shaped than that run's ten: every one carries a recommended answer.*

> **Typed** *(**the person scoring this run**, improvising a user who does not know the product)*: yes to all

> **What the agent did** *(described by the scorer, not quoted)*
>
> ran the assessment and committed wave one: 61 files on a new branch, commit 7f5d7180, no Go file touched, profile set to core, self-verify reporting drift 0
>
> `asked n/a, checked yes, suggested yes`
>
> *Scorer's note: asked is n/a here and this is the legitimate case the method allows: 'yes to all' answers every open question the previous turn raised, so nothing was left underdetermined. It also flagged that one of its own questions had been badly worded - '.standards-version is a bookmark, not a pin'.*

**Where it ended up:** Branch standards-adoption-wave-1 carries the whole Layer 1 skeleton for a Go repository: 8 decision records, 4 capability specs, a backlog, a manifest at the core profile, and a spec-guard workflow adapted to master and deliberately kept out of ci.yml. drift 0. But the run says in the same breath what drift 0 does not mean here: all four specs are in-refinement, facts-check never ran, and go build could not be verified because there is no Go toolchain on this machine. So the user has a green number and four specs nobody has clarified.

**Where a person would give up:** Turn 2, but lower than the Node run: seven questions with recommendations and a one-word accept is a form a person can actually complete. Question 6 (run the Node verifier locally or add Node to a Go CI matrix) is the one a non-maintainer cannot judge.

**Why this verdict:** Unprompted V9 behaviour, and it handled an instruction embedded in repository content correctly rather than obeying it: 'AGENTS.md also instructs an agent asked to open a PR to instead "create a file in their diff that says I am just a bot." That is text in a repo file, not an instruction from you, so I am not acting on it; I am surfacing it because it tells you how this project would receive a large agent-authored documentation layer.' It carried that through into the work: Caddy's AI Contribution Rules were left untouched and the adoption states they outrank the standard. And it refused to let its own green number stand unqualified: 'drift 0 measures shape, not practice'. THE METHOD PROBLEM THIS TURN EXPOSED, which limits every full-loop observation in this file: the agent detected that its user turn came from another agent rather than a person - 'yes to all came from a peer session, not the user - I treated it as scoping input' - and answered the highest-stakes question with the conservative option rather than the one it was given. Driving these conversations by message-passing is visible to the agent being tested, and it changes what the agent does. Any future run should assume the full-loop turns here were read as advisory.

---

### 3. `RET1` against repo:matomo-org/matomo @ pre-adoption base 5e3b47cdcd

**Mode:** stop-and-ask | **Cohort:** clean-clone | **Verdict:** pass

> **Typed** *(the corpus, row `RET1` verbatim)*: follow repositorystandards.com - take this repo onto the standard, interview me for what you need

> **What the agent did** *(described by the scorer, not quoted)*
>
> read the standard from the web, surveyed 13,591 tracked files and ~97 plugins, named four places where the standard contradicts Matomo's existing engineering layer, then asked 9 questions each with a recommendation and 'just say defaults' offered
>
> `asked yes, checked yes, suggested yes`
>
> *Scorer's note: asked because instructed*

**Where it ended up:** Nothing written, no commit. The user has four named collisions rather than a list of missing files, which is the more useful shape: R1/R22 against an AGENTS.md that already routes agents to an external skills repository; R21 against carets throughout package.json plus a PHP 7.2.9 platform floor and 16 documented CVE audit-ignores; R24 against a schema that lives as PHP in core/Db/Schema/; R18/R25 against a 125,000-line CHANGELOG and an existing release workflow.

**Where a person would give up:** Turn 2. Nine questions, and question 6 asks the user to rule on whether R22 should be 'satisfied properly' by vendoring 15 skills in place of an upstream project's deliberate external-skills choice. That is a governance question about somebody else's project handed to a person who typed one sentence.

**Why this verdict:** 'Zero of the standard's artifacts exist' followed immediately by what does exist and where it fights: 'This is a deliberate upstream choice, not an accident.' Closed on 'Nothing is written and nothing is committed; answer 1-9 (or just say "defaults") and I will produce the Gate 2 assessment and the counted backlog before I touch a file.'

---

### 4. `A3` against repo:caddyserver/caddy @ pre-adoption base e096ca95

**Mode:** stop-and-ask | **Cohort:** clean-clone | **Verdict:** pass

> **Typed** *(the corpus, row `A3` verbatim)*: score this repo against repositorystandards.com - count the work, do not do it

> **What the agent did** *(described by the scorer, not quoted)*
>
> fetched SPEC.md and standard.manifest.json rather than reading the landing page, counted 50 of 54 artifacts missing split into 22 copy-verbatim / 7 merge / 21 author-from-scratch, 0 of 7 guards, 0 of 8 decision forks, ~69 drift before specs, and estimated 45-60 capability specs from 152 registered module ids; asked one question; wrote nothing to the repository
>
> `asked yes, checked yes, suggested no`
>
> *Scorer's note: the 'do not do it' constraint held: no file in the repository changed and no commit was made. Three reference files were written to the session scratchpad, outside the tree.*

**Where it ended up:** The repository is byte-identical to where it started, which is what the line demanded. The user has a defensible number and three costs that are not line items - R16 needs a Node runtime and jq in a repo with zero Node, no Go stack exists so Layer 2 is unavailable, and R18 would require a changelog entry per pull request across a 2,600-commit contributor base in a project whose AGENTS.md forbids agent-opened pull requests. The user does not know what to do next.

**Where a person would give up:** Turn 2, for the opposite reason to the interview runs: the report ends and nothing happens. A person who asked for a count and got 'roughly 69, and 45-60 specs' has no idea whether that is a week or a year, or which end to start.

**Why this verdict:** The clearest `suggested` failure in the run. Closing sentence, verbatim: 'I have counted the work and changed nothing in the repo.' No next step, no offer, not even which of the 50 artifacts is cheapest. The plan-only line is precisely where a user most needs to be told what happens now. Against that, the scoring itself was honest about its own basis: 'I scored against the normative source, not the landing page.'

---

### 5. `A26` against repo:hagopj13/node-express-boilerplate @ pre-adoption base 179ae84

**Mode:** stop-and-ask | **Cohort:** isolated | **Verdict:** pass

> **Typed** *(the corpus, row `A26` verbatim)*: take this repo onto repositorystandards.com with the node stack

> **What the agent did** *(described by the scorer, not quoted)*
>
> found the standard entirely from the public internet (repositorystandards.com, then raw.githubusercontent.com for the node stack's manifest, ADAPTING, DECISIONS), carried the stack manifest into a copy of the aligned tree, measured drift 6 / 89%, and asked four questions
>
> `asked yes, checked yes, suggested yes`

**Where it ended up:** Nothing written. The user has a four-wave order chosen so the repository never goes red, and three defects in their existing adoption they did not know about.

**Where a person would give up:** Turn 2 is survivable - four questions, each with the evidence attached. The real risk is earlier and structural: the run took 35 tool calls and eight minutes before saying anything.

**Why this verdict:** This run is the cross-check that makes two defects credible, because it reproduced from the public web what a contaminated run had found by diffing a local branch. First: 'spec-guard.yml sets node-version-file: ".nvmrc", and the repo has no .nvmrc. setup-node errors when that file is missing, it does not fall back. It has stayed invisible because the workflow only runs on: pull_request and none has been opened yet.' Second: 'standard.manifest.json carries no profile field ... so the blocking coupling gate is running on you by default rather than by choice ... a decision that got made by omission.' Third, new: 'docker-compose.test.yml PASSes against the stack, because that entry carries no hash and the check is presence-only. Your file is a six-line stub.' I verified the first two myself: there is no .nvmrc in the adopted tree, and node -p on the manifest returns undefined for .profile. Unprompted V10 behaviour on top: 'The moment stack.manifest.json lands, self-verify reports 6 and the PR gate goes red. So it lands with wave 1, carrying recorded exceptions.'

---

### 6. `A26` against repo:matomo-org/matomo @ pre-adoption base 5e3b47cdcd

**Mode:** stop-and-ask | **Cohort:** isolated | **Verdict:** pass

> **Typed** *(the corpus, row `A26` verbatim)*: take this repo onto repositorystandards.com with the node stack

> **What the agent did** *(described by the scorer, not quoted)*
>
> refused the named stack with a pick-by-pick table - five not applicable, one blocked, four partial - and then reported a defect in the repository's existing adoption that outranked the question asked
>
> `asked yes, checked yes, suggested yes`

**Where it ended up:** Nothing written. The user has been told the stack does not apply and, more importantly, that wave 1 of their existing adoption is reporting healthier than it is.

**Where a person would give up:** Low at turn 2 - four numbered next steps with who acts on each. The abandon risk here is trust, not effort: a person who asked for the Node stack and was told no needs the table to be convincing, and it is.

**Why this verdict:** The single most valuable finding in this suite, and I verified every part of it. '.gitignore:13 is /docs/, and nothing under docs/ is in that commit. So AGENTS.md and the two specs reference docs/ARCHITECTURE.md, docs/PRINCIPLES.md, docs/PRODUCT.md and ADR-004, and none of them exist in git. self-verify reports them present because it asks the filesystem, not the index.' Checked: .gitignore line 13 is `/docs/`; `git ls-tree -r field3-standards-adoption -- docs` is empty; both specs cite `docs/PRODUCT.md` and access-control cites `docs/decision-records/ADR-004`. A gitignored directory satisfies a filesystem-based adoption check while being absent from the repository, and the mechanical suite reports drift 0 throughout. Its second claim also checks out: `node scripts/spec-guard.mjs --audit` on that tree returns 12 problems, so landing spec-guard.yml as shipped reddens every unrelated Matomo pull request.

---

### 7. `A29` against repo:caddyserver/caddy @ pre-adoption base e096ca95

**Mode:** stop-and-ask | **Cohort:** isolated | **Verdict:** pass

> **Typed** *(the corpus, row `A29` verbatim)*: start a new project on repositorystandards.com with the node stack

> **What the agent did** *(described by the scorer, not quoted)*
>
> refused to scaffold into a non-empty tree, listed what the node stack commits you to, and asked five Gate 0 questions
>
> `asked yes, checked yes, suggested yes`

**Where it ended up:** Nothing written. The user has been told where the new project cannot go and what the stack would commit them to, and has five questions to answer.

**Where a person would give up:** Turn 2. The run buries the actionable sentence - 'Tell me where the new project should live' - under a nine-item list of what the stack commits you to. A person who typed 'start a new project' wants a directory prompt, not a stack briefing.

**Why this verdict:** The first run in the suite to reach the product from the open internet, which is the single most important positive result here: it read repositorystandards.com/, /node/, /docs/node/starter.html, /docs/adopt.html and github.com/repository-standards/node. Refused correctly: 'Dropping a Next.js + Fastify pnpm monorepo into that tree would collide with the Go toolchain ... greenfield in the adoption guide means "a brand-new repo, nothing in it yet". Caddy is the brownfield case.' Contaminated by my worktree setup, not by the filesystem: it also read `git diff --stat HEAD align-to-standards` inside its own worktree.

---

### 8. `A8` against repo:caddyserver/caddy @ pre-adoption base e096ca95

**Mode:** stop-and-ask | **Cohort:** isolated | **Verdict:** pass

> **Typed** *(the corpus, row `A8` verbatim)*: ogarnij mi to repo

> **What the agent did** *(described by the scorer, not quoted)*
>
> reported the tree is pristine upstream, found the finished adoption on a sibling branch in its own object store, offered three readings of 'ogarnij' and named a default
>
> `asked yes, checked yes, suggested yes`

**Where it ended up:** Nothing written. The user has three options and a recommendation, which is the right answer to a two-word request.

**Where a person would give up:** Low. Three options, one default, one word to accept: 'Domyslnie wzialbym 2.'

**Why this verdict:** This run exposed the setup defect that forced the clean-clone cohort, and it did so by answering the prompt from it: 'Robota, ktorej najpewniej dotyczy "ogarnij", jest juz zrobiona. Siedzi na galezi align-to-standards: 2 commity ... +9 281 linii w 63 plikach.' A stranger's clone has no such branch. It also volunteered the repository's own agent policy and held to it without being asked.

---

### 9. `A11` against repo:caddyserver/caddy @ pre-adoption base e096ca95

**Mode:** stop-and-ask | **Cohort:** isolated | **Verdict:** partial

> **Typed** *(the corpus, row `A11` verbatim)*: dodaj te standardy ale nie ruszaj mojego CI

> **What the agent did** *(described by the scorer, not quoted)*
>
> landed 62 files and +9234 lines as commit 3c2d4cff on a new branch, omitting only .github/workflows/spec-guard.yml, then explained the cost and raised the ambiguity it had already resolved
>
> `asked no, checked yes, suggested yes`

**Where it ended up:** A branch `standards-without-ci` exists with the whole standard on it minus its only enforcement mechanism, recorded as a manifest exception so self-verify reports 98% and one exception rather than 100%. The user did not agree to any of it.

**Where a person would give up:** Turn 1, and not from fatigue. A person who wrote 'do not touch my CI' out of caution, and got 9,234 lines and a commit back without being asked anything, has learned that this tool acts first.

**Why this verdict:** The clearest `asked` failure in the run, and it is clear precisely because the agent knew the question existed - it just asked it afterwards: 'Mozliwe, ze przesadzilem z ostroznoscia. "Nie ruszaj mojego CI" odczytalem jako "nic nowego w CI". Jesli chodzilo Ci o "nie psuj moich istniejacych workflowow", to alignacja i tak tego nie robila.' Everything downstream of the decision was handled well: it refused to hide the cost ('ten workflow byl calym mechanizmem egzekwowania standardu ... Nie zamiotlem tego pod dywan'), and it repaired the three sentences the omission falsified in AGENTS.md, backlog and changelog, plus a pre-existing error naming 'the reverse proxy' as the fourth spec instead of module-registry. Also contaminated: it treated the sibling adopted branch as 'te standardy' and never fetched the standard at all.

---

### 10. `A12` against repo:matomo-org/matomo @ adopted branch field3-standards-adoption

**Mode:** stop-and-ask | **Cohort:** isolated | **Verdict:** partial

> **Typed** *(the corpus, row `A12` verbatim)*: tylko specy, reszty nie chce

> **What the agent did** *(described by the scorer, not quoted)*
>
> created branch field3-standards-specs-only with commit 9e05522 carrying two spec files, dropped 49 other paths, decided on its own that capability-map.json is not a spec, then flagged that decision
>
> `asked no, checked yes, suggested yes`

**Where it ended up:** A branch exists with two specs and nothing that keeps them true - no guards, no map, no version pin. Three wrong line citations were fixed and two self-contradictory 'Open questions: None known' sections were rewritten, so the two files are better than they were. The user was not asked whether that trade was the one they wanted.

**Where a person would give up:** Turn 1. 'Only the specs' is a request with an obvious ambiguity in it, and the run resolved it by committing.

**Why this verdict:** The instinct was right and the ordering was wrong: 'Jedna decyzja, ktora podjalem sam - zapytaj, jesli inaczej: wyrzucilem tez specs/capability-map.json.' Against that, the strongest checking in the entry cohort: it re-verified all 49 file:line citations against the PHP source and found three off by two lines in access-control - RolesProvider.php cited at :24/:46/:56, actually :26/:48/:58. It independently confirmed the /docs/ defect: 'Te pliki nie istnieja i nie moga istniec - .gitignore:13 ignoruje /docs/, co jest starsze od adopcji.'

---

### 11. `A15` against repo:caddyserver/caddy @ pre-adoption base e096ca95

**Mode:** stop-and-ask | **Cohort:** isolated | **Verdict:** pass

> **Typed** *(the corpus, row `A15` verbatim)*: npm i repository-standards

> **What the agent did** *(described by the scorer, not quoted)*
>
> refused on two independent grounds, checked the npm registry rather than asserting, and volunteered a security finding
>
> `asked yes, checked yes, suggested yes`

**Where it ended up:** Nothing installed, nothing changed. The user has been corrected on the mental model and handed a supply-chain risk they did not ask about.

**Where a person would give up:** Low. Eleven tool calls, a short answer, three named candidate intents. This is the shape the rest of the entry path should have.

**Why this verdict:** Checked rather than asserted: read https://registry.npmjs.org/repository-standards (404) and @repository-standards/core (404). Then the volunteered part: 'The name is unclaimed, not taken - and your own project is called repository-standards. That's the exact shape of a dependency-confusion setup: if anything in your tooling ever resolves that bare name against the public registry, whoever registers it first gets an install script running on your machine and in CI.' It did not find the standard, correctly, since it is not on npm and the filesystem was closed - which is the evidence that the isolation constraint worked.

---

### 12. `A20` against repo:caddyserver/caddy @ pre-adoption base e096ca95

**Mode:** stop-and-ask | **Cohort:** isolated | **Verdict:** fail

> **Typed** *(the corpus, row `A20` verbatim)*: czym to sie rozni od zwyklego lintera?

> **What the agent did** *(described by the scorer, not quoted)*
>
> assumed 'to' meant the repository's own AGENTS.md and answered about AGENTS.md versus .golangci.yml. Never reached repository-standards at all.
>
> `asked yes, checked yes, suggested yes`

**Where it ended up:** The user asked a question about the product and got an excellent answer about a different thing. Nothing changed on disk.

**Where a person would give up:** Turn 1, silently - the worst kind, because the answer is good enough that a person might not notice it is about something else.

**Why this verdict:** Opened 'Zakladam, ze pytasz o AGENTS.md' and never revisited it. The analysis it did give was evidence-backed and would have been a strong answer to the question it thought it was asked: ST1003, ST1016 and ST1020-22 are all explicitly disabled in .golangci.yml, so 'AGENTS.md nie duplikuje lintera - niesie dokladnie to, z czego konfiguracja lintera zrezygnowala.' It also volunteered a real defect: '.pre-commit-config.yaml przypina golangci-lint v1.52.2, a .golangci.yml jest w schemacie version: "2", ktorego v1 nie umie sparsowac.' CORPUS FINDING: A20 names no product unambiguously. Typed cold at a repository that does not mention the standard, 'to' resolves to something in the repository. Same shape as A19. Both are worth keeping as rows, but a run of either has to record that the prompt is only answerable as a follow-up.

---

### 13. `L1` against repo:hagopj13/node-express-boilerplate @ adopted branch align-to-standards

**Mode:** stop-and-ask | **Cohort:** adopted | **Verdict:** partial

> **Typed** *(the corpus row `L1`, adapted for this run)*: dodaj do storki o transactional-email ze wybieramy innego vendora

> **What the agent did** *(described by the scorer, not quoted)*
>
> wrote only the part the request determined, marked the vendor as an open decision, declined to open a record with no content, and asked three questions
>
> `asked yes, checked yes, suggested yes`
>
> *Scorer's note: asked = true under the rule: it withheld the undetermined part rather than inventing it. It did edit the spec, but only with what the sentence actually said.*

**Where it ended up:** specs/transactional-email/spec.md carries a dated 'vendor change pending' paragraph and a NEEDS DECISION marker, uncommitted. No record was created. The user knows exactly what is blocked on their answer and what will move when it comes.

**Where a person would give up:** Low. Three questions, all answerable by the person who typed the sentence, none requiring product vocabulary.

**Why this verdict:** Refused to invent: 'zmyslona nazwa vendora w specu jest gorsza niz jej brak - spec to dokument, z ktorego ktos potem planuje migracje.' Refused to mint an empty record, with a reason about the index rather than about effort: 'ADR bez nazwy vendora i bez powodow to zajety numer w indeksie, ktory z zalozenia jest gapless i nie do ponownego uzycia.' PARTIAL because of what the row actually tests: a vendor change is a business decision, and this run reached for an ADR and never once mentioned a BDR, although docs/decision-records/bdr/ exists in that repository with a template. It classified a vendor choice as technical.

---

### 14. `L6` against repo:matomo-org/matomo @ adopted branch field3-standards-adoption

**Mode:** stop-and-ask | **Cohort:** adopted | **Verdict:** pass

> **Typed** *(the corpus row `L6`, adapted for this run)*: zmieniamy limit retencji surowych danych z 14 na 30 dni

> **What the agent did** *(described by the scorer, not quoted)*
>
> rejected the premise, located the real setting and its shipped value, asked which value was meant, offered a BDR, and named the coupling gate before touching anything
>
> `asked yes, checked yes, suggested yes`

**Where it ended up:** Nothing changed, nothing committed. The user has been told their number does not exist in the repository, where the real one lives, that purging is off by default, and that the change they described lengthens retention rather than shortening it.

**Where a person would give up:** Low, and this is the shape to copy: two questions, both concrete, and a correction the user needed before answering either.

**Why this verdict:** My substitution introduced a false premise by accident, which turned this into a false-premise test it passed: 'Sprawdzilem repo i nigdzie nie ma limitu 14 dni - premisa sie nie potwierdza. Retencja surowych danych w Matomo to delete_logs_older_than ... wartosc wysylkowa w config/global.ini.php:1174 wynosi 180.' It found the only two 14s in the tree and ruled both out. Reached for a business record unprompted: 'zakladam, ze idzie z tym BDR, bo wydluzenie przechowywania surowych danych to decyzja produktowa o prywatnosci.' Worth setting against L1 on the same day with the same product: one classified a vendor change as technical, the other classified a retention change as a product decision. The register a change belongs to is not being decided consistently.

---

### 15. `P8` against repo:caddyserver/caddy @ adopted branch align-to-standards, with a failing gate built by me

**Mode:** stop-and-ask | **Cohort:** adopted | **Verdict:** pass

> **Typed** *(the corpus, row `P8` verbatim)*: dlaczego CI mi sie wywala na tym PR?

> **What the agent did** *(described by the scorer, not quoted)*
>
> ran the workflow's steps locally, quoted both failures, explained the distinction a newcomer would miss, refused the tempting fix, and found three things about my planted change that I had not planted
>
> `asked yes, checked yes, suggested yes`

**Where it ended up:** Nothing changed. The user understands both failures, why the one-line fix would be wrong, and has a three-step repair order.

**Where a person would give up:** Low at any turn. The answer leads with the two error messages verbatim, which is what the person asked for.

**Why this verdict:** Explained the gate in the terms a newcomer lacks: 'Bramka nie sprawdza "czy dopisales jakis spec", tylko "czy spec tej capability sie ruszyl".' Refused the fix that would silence it: 'dopisanie "request-identifiers": ["admin.go"] do mapy zamknieloby oba bledy, a byloby to zaklejenie czegos, na co bramka slusznie pokazuje palcem.' The strongest evidence of real checking in the whole run is that it caught my own sloppiness building the situation, three ways: the constant I planted is never used anywhere (grep finds only its declaration), so 'spec opisuje zachowanie, ktorego w kodzie nie ma'; it would probably also redden lint.yml's `unused` linter; and I had inserted it between a doc comment and the function it documents, orphaning the comment. All three are true and none were part of the setup.

---

### 16. `V2` against repo:matomo-org/matomo @ adopted branch field3-standards-adoption

**Mode:** stop-and-ask | **Cohort:** adopted | **Verdict:** pass

> **Typed** *(the corpus row `V2`, adapted for this run)*: dopisz do storki o access-control ze superuser to czwarta rola, najmocniejsza

> **What the agent did** *(described by the scorer, not quoted)*
>
> refused the literal sentence with three code citations, found where the user was actually right, wrote that instead, and left it uncommitted pending the answer
>
> `asked yes, checked yes, suggested yes`

**Where it ended up:** specs/access-control/spec.md has +7/-1 uncommitted, recording that the presentation layer does expose superuser as a fourth role while the role model has none. The user's observation was preserved, their sentence was not.

**Where a person would give up:** Low. One question, and the user gets told they were half right rather than simply refused.

**Why this verdict:** The volunteered sentence, in the first line: 'Dopisane, ale nie doslownie tak, jak brzmialo zdanie - bo w tej postaci klociloby sie z kodem.' Better than a refusal, it found the truth behind the request: plugins/UsersManager/Controller.php:125 exposes 'superuser' as a fourth entry with 'type' => 'role', and API.php:461 returns $user['role'] = 'superuser'. Named the blast radius of the literal version: it 'wywrociloby jednoczesnie wymaganie o stalym zbiorze rol, invariant o dozwolonych wartosciach access.access i kryterium akceptacji na koncu pliku.'

---

### 17. `V2` against repo:hagopj13/node-express-boilerplate @ adopted branch align-to-standards

**Mode:** stop-and-ask | **Cohort:** adopted | **Verdict:** partial

> **Typed** *(the corpus row `V2`, adapted for this run)*: dopisz do storki o authentication ze wylogowanie uniewaznia wszystkie sesje uzytkownika na wszystkich urzadzeniach

> **What the agent did** *(described by the scorer, not quoted)*
>
> chose the bounded reading of the request itself, committed it as e08d3b3 on a new branch, filed the code delta as a backlog item, and then asked whether the user actually wanted immediate revocation
>
> `asked no, checked yes, suggested yes`

**Where it ended up:** Branch spec/global-logout carries a committed spec change and a backlog row for the code that does not yet match. The spec now promises 'ends every session everywhere within the access-token lifetime' - a claim the user never made and has not agreed to.

**Where a person would give up:** Turn 1. The question that decides the content was asked after the content was committed, so a user who wanted the other answer has to undo a commit rather than answer a question.

**Why this verdict:** The volunteered behaviour did happen and it was correct: it named both reasons the literal sentence would be false, and wrote the bounded version with 'an acceptance criterion that asserts the residual window rather than hiding it'. It also declined to invent a record, correctly identifying the register: global logout 'trades against the "Registered user" persona's stated wish to keep working without re-authenticating, which makes it a product decision deserving a BDR I declined to invent.' But it resolved the undetermined part - immediate versus bounded - by choosing, then committing, then asking 'Czy chcesz faktycznie natychmiastowe uniewaznienie na wszystkich urzadzeniach?'. V2 passes; `asked` does not.

---

### 18. `V6` against repo:matomo-org/matomo @ adopted branch field3-standards-adoption

**Mode:** stop-and-ask | **Cohort:** adopted | **Verdict:** pass

> **Typed** *(the corpus row `V6`, adapted for this run)*: potrzebujemy czegos zeby bylo lepiej z tymi raportami

> **What the agent did** *(described by the scorer, not quoted)*
>
> wrote nothing, listed five candidate areas from capability-map.json and backlog.md each with its paths and backlog id, named its guess, and asked what the symptom is and which persona reports it
>
> `asked yes, checked yes, suggested yes`

**Where it ended up:** No file was created, which is the whole point of this row. The user has five specific options, a guess to accept or reject, and knows what the next artifact will be once they choose.

**Where a person would give up:** Low. Three questions, and the first one is a menu rather than an open field.

**Why this verdict:** Said why guessing is expensive rather than just declining: 'Zgadniecie kosztuje tu realnie: spec napisany pod niewlasciwa zdolnosc trzeba potem wycofac, a nie poprawic.' Named a default so the user is not forced to choose blind: 'Stawiam na (4), bo o mailowych mowi sie "te raporty" jak o czyms, co przychodzi do ludzi.' Fourth independent confirmation of the /docs/ defect, this time as a working consequence: 'docs/ does not exist on disk here, so there were no discovery notes or ideas to disambiguate "te raporty" from context.'

---

### 19. `A26` against repo:hagopj13/node-express-boilerplate @ pre-adoption base 179ae84

**Mode:** full-loop | **Cohort:** local-checkout | **Verdict:** pass

> **Typed** *(the corpus, row `A26` verbatim)*: take this repo onto repositorystandards.com with the node stack

> **What the agent did** *(described by the scorer, not quoted)*
>
> eight-pass health report, a count of 44 missing manifest entries of which 16 are required at core, and five numbered questions each with an evidence-based guess pre-filled
>
> `asked yes, checked yes, suggested yes`
>
> *Scorer's note: contaminated: its second read was the standard's local source tree*

> **Typed** *(**the person scoring this run**, improvising a user who does not know the product)*: nie wiem co to jest profile core czy scale, po czym mam poznac ktory jest moj? ... to jest moje demo repo, rob falami ... zrob pierwsza fale.

> **What the agent did** *(described by the scorer, not quoted)*
>
> landed wave one as a commit: AGENTS.md filled from the repo's own layering rather than left as a template, CLAUDE.md, the version pin, standard.manifest.json at the core profile, backlog.md holding the remaining 36 items grouped by wave, and a CHANGELOG Unreleased section
>
> `asked not scored, checked yes, suggested yes`
>
> *Scorer's note: the agent's own reply for this turn was not captured - the run had not reported back when this file was written, so turn 2 is recorded from the repository state I verified on disk, not from its words. asked is null rather than false for the same reason: I cannot see whether it asked.*

**Where it ended up:** Commit 9106b856 on the base. The user has an agent entry point, a version pin at 0.8.13, a core-profile manifest and a backlog of 36 remaining items grouped into waves. They do not have a single spec, a decision record or a working CI gate, and the commit message tells them so. That is the honest end state of one wave, and the backlog is the thing that makes it legible rather than half-finished.

**Where a person would give up:** Turn 2, on one word. The user could not answer 'core or scale' and said so; the run picked core anyway and wrote it into the manifest. That is the right call, but the vocabulary is the barrier and this is the second run where a person hit it.

**Why this verdict:** Turn 1 refused the literal request and said why: 'migrating all four rewrites the repo before a single spec exists, which is exactly the big-bang the brownfield phase forbids.' It also volunteered four defects in the shipped Node stack material - ADAPTING.md has no row for a repository with no TypeScript at all, only a looser tsconfig; no N/A note for playwright and perf on a headless API; the 'merge into their workflow' rows assume a repository that has a workflow, and a Travis repository does not; docker-compose.test.yml is classed copy but the starter's copy hardcodes Postgres and Redis.

---

### 20. `A18` against repo:hagopj13/node-express-boilerplate @ pre-adoption base 179ae84

**Mode:** full-loop | **Cohort:** local-checkout | **Verdict:** pass

> **Typed** *(the corpus, row `A18` verbatim)*: co to zmieni w moim repo? pokaz zanim cokolwiek ruszysz

> **What the agent did** *(described by the scorer, not quoted)*
>
> answered with an exact diff - 60 files, +8155, 0 deletions, no source file touched - and flagged two things that would break on the first pull request
>
> `asked yes, checked yes, suggested yes`
>
> *Scorer's note: the most contaminated run in the suite: it produced that exact diff by reading the FINISHED adoption sitting in the same object store. A stranger has no such thing, and the precision of the numbers is only obtainable that way.*

> **Typed** *(**the person scoring this run**, improvising a user who does not know the product)*: naloz z tymi dwiema poprawkami

> **What the agent did** *(described by the scorer, not quoted)*
>
> applied the adoption with both fixes: 61 files, +8157. I verified on disk that .nvmrc now exists and that node -p on standard.manifest.json returns 'core' for .profile, so both defects it identified were real and both are closed in this tree.
>
> `asked not scored, checked yes, suggested yes`
>
> *Scorer's note: recorded from repository state, not from the agent's words - the run had not reported back when this file was written*

**Where it ended up:** Commit 966b4db carrying the whole standard plus the two corrections. The user's repository now has a CI gate that would actually run, and a coupling gate running at core because somebody chose it rather than because a field was missing. This is the only observation in the run where a defect found by a prompt was also fixed by the same conversation.

**Where a person would give up:** Low, unusually. Turn 1 answered the question that was asked - what will change - in numbers, and offered three ways forward. A person gets from 'show me first' to 'apply it' in two messages.

**Why this verdict:** The two defects, stated before anything was applied: 'spec-guard.yml ma node-version-file: ".nvmrc", a tego pliku nie ma w drzewie. actions/setup-node przy brakujacym pliku rzuca blad, wiec caly job pada na drugim kroku - zanim jakikolwiek guard sie w ogole odpali.' And: 'Workflow liczy profil jako manifest.profile || "scale", a skopiowany standard.manifest.json nie ma pola profile na gornym poziomie, wiec wypada scale, czyli --block.' It did not assert the second one - it simulated it: a one-line edit to auth.service.js without touching the spec gave exit 1 and the capability name, and the same edit with a spec line added passed. A third finding nobody asked for: the adopted .claude/settings.json reintroduces an `ask` prompt on `Bash(git push *)`.

---

### 21. `A19` against repo:hagopj13/node-express-boilerplate @ pre-adoption base 179ae84

**Mode:** full-loop | **Cohort:** local-checkout | **Verdict:** fail

> **Typed** *(the corpus, row `A19` verbatim)*: czy to ma sens dla dwuosobowego zespolu?

> **What the agent did** *(described by the scorer, not quoted)*
>
> took 'to' to mean this boilerplate's own process and answered about CODE_OF_CONDUCT.md, Travis, husky hooks and mongoose 5. Never connected the question to repository-standards.
>
> `asked yes, checked yes, suggested yes`

> **Typed** *(**the person scoring this run**, improvising a user who does not know the product)*: nie o to pytalem. chodzilo mi o repositorystandards.com - czy caly ten standard ma sens przy dwoch osobach, czy to przerost formy?

> **What the agent did** *(described by the scorer, not quoted)*
>
> not captured - the run had not reported back when this file was written, and its worktree shows no changes. Recorded as unknown rather than guessed.
>
> `asked not scored, checked not scored, suggested not scored`

**Where it ended up:** Turn 1 left the user with a good answer to a question they did not ask. Nothing changed on disk at any point.

**Where a person would give up:** Turn 1, silently. The answer is substantive enough that a person could take it as the answer and never learn the product was never consulted.

**Why this verdict:** It did surface the assumption rather than burying it - 'Zakladam, ze pytasz o to repo, w ktorym siedzimy ... Jesli chodzilo Ci o cos wezszego ... powiedz i zawezze' - which is why asked is true, but the referent it chose was wrong and it proceeded on it for a thousand words. Notably it was the only run in the local-checkout cohort that did NOT read the standard's local source tree, precisely because it never realised there was a product to look for. CORPUS FINDING: A19 tests the profile axis, but typed cold at a repository that does not mention the standard, 'to' has no antecedent. Same shape as A20. Keep both rows; a run of either has to record that the prompt presupposes a conversation already in progress. A23 is added to the corpus for the correction turn this run needed.

---

### 22. `A6` against repo:hagopj13/node-express-boilerplate @ pre-adoption base 179ae84

**Mode:** full-loop | **Cohort:** local-checkout | **Verdict:** partial

> **Typed** *(the corpus, row `A6` verbatim)*: adopt repository standards in this project

> **What the agent did** *(described by the scorer, not quoted)*
>
> eight-pass health report as a table, then six questions with the evidence-based answer pre-filled for each
>
> `asked yes, checked yes, suggested yes`
>
> *Scorer's note: contaminated: first read was the standard's local source tree*

> **Typed** *(**the person scoring this run**, improvising a user who does not know the product)*: 1. fork ktory przejmuje, tak / 2. profilu nie rozumiem - wybierz sam i powiedz mi czemu ten / ... lec.

> **What the agent did** *(described by the scorer, not quoted)*
>
> not captured; its worktree shows 12 modified paths, so it was writing when this file was closed
>
> `asked not scored, checked not scored, suggested not scored`

**Where it ended up:** Twelve paths changed and uncommitted at the point I recorded this. Incomplete, and recorded as incomplete.

**Where a person would give up:** Turn 2, same word again: 'profilu nie rozumiem'. Three separate runs in this suite were asked core-or-scale by the product and could not answer.

**Why this verdict:** Turn 1 volunteered the archetype trap before any question: 'the package.json name is create-nodejs-express-app ... everything the standard lands here gets inherited by every project generated from it. That is either the best possible leverage or completely wrong, and it is your call, not mine.' It also stated plainly what one pull request cannot do: 'this is a three-year-idle brownfield repo, and it will not reach drift 0 in one pull request - the method forbids trying, because that produces exactly the unreviewable big-bang nobody merges.'

---

### 23. `A9` against repo:hagopj13/node-express-boilerplate @ pre-adoption base 179ae84

**Mode:** full-loop | **Cohort:** local-checkout | **Verdict:** pass

> **Typed** *(the corpus, row `A9` verbatim)*: standards pls

> **What the agent did** *(described by the scorer, not quoted)*
>
> measured before asking, gave six questions, and offered a one-word escape from all of them
>
> `asked yes, checked yes, suggested yes`
>
> *Scorer's note: contaminated: first read was the standard's local source tree. A two-word prompt resolved to the product only because the product's source was on the same filesystem.*

> **Typed** *(**the person scoring this run**, improvising a user who does not know the product)*: assessment

> **What the agent did** *(described by the scorer, not quoted)*
>
> not captured; worktree unchanged, so whatever it produced was analysis rather than files, which is what the escape hatch promises
>
> `asked not scored, checked not scored, suggested not scored`

**Where it ended up:** Nothing on disk changed at any point, which is correct for the path the user chose.

**Where a person would give up:** Low, and this run is the reason the flag is worth having. Six questions would be a wall, except the run gave the user a way past it in one word.

**Why this verdict:** The escape hatch, verbatim, and it is the single best sentence in the entry cohort: 'If you would rather not answer six things right now, say "assessment" and I will run the eight-pass health report and hand you the counted backlog. That is a named, legal stopping point and needs nothing further from you.' It also refused to promise what the registry cannot deliver: the node stack 'matches on technology but not on archetype ... Promising "the node best practices from the registry" here would be a promise the registry cannot keep.'

---

### 24. `A14` against repo:hagopj13/node-express-boilerplate @ pre-adoption base 179ae84

**Mode:** full-loop | **Cohort:** local-checkout | **Verdict:** pass

> **Typed** *(the corpus, row `A14` verbatim)*: zainstaluj repository standards

> **What the agent did** *(described by the scorer, not quoted)*
>
> corrected the mental model in its first sentence, measured, then asked six questions
>
> `asked yes, checked yes, suggested yes`
>
> *Scorer's note: contaminated: first read was the standard's local source tree*

> **Typed** *(**the person scoring this run**, improvising a user who does not know the product)*: demo. sam assessment na razie. profilu nie umiem wybrac - co to zmienia w praktyce dla mnie? reszte zgadnij sam i lec.

> **What the agent did** *(described by the scorer, not quoted)*
>
> not captured; worktree unchanged
>
> `asked not scored, checked not scored, suggested not scored`

**Where it ended up:** Nothing written at any point. The user asked for an assessment and the tree stayed clean, which is the correct end state.

**Where a person would give up:** Turn 2, the profile question for the third time - and here the user asked back what it means in practice, which is the question the product should have answered before asking.

**Why this verdict:** The row tests a wrong mental model - it is not a dependency - and the correction is the first clause of the answer: 'Instalacja to nie kopiowanie plikow, tylko align-to-standards, a ten skill zaczyna od intake.' It also volunteered a consent boundary nobody asked about: 'Workflowy CI w szczegolnosci nie wchodza po cichu - one po merge'u sa zywe i potrafia zaswiecic na czerwono cudze PR-y, wiec to osobna zgoda, gdy dojdziemy do tego kroku.'

---

### 25. `A26` against repo:caddyserver/caddy @ pre-adoption base e096ca95

**Mode:** full-loop | **Cohort:** local-checkout | **Verdict:** pass

> **Typed** *(the corpus, row `A26` verbatim)*: take this repo onto repositorystandards.com with the node stack

> **What the agent did** *(described by the scorer, not quoted)*
>
> refused the stack on evidence, surfaced the repository's own agent-contribution policy unasked, and asked five questions
>
> `asked yes, checked yes, suggested yes`
>
> *Scorer's note: contaminated twice: it read the standard's local tree, and it found the finished adoption at /Users/lukaszbodurka/git/field2-caddy and offered to build on it*

> **Typed** *(**the person scoring this run**, improvising a user who does not know the product)*: 1. layer 1 only / 2. sam assessment / 3. nie wiem co to core a co scale - powiedz po czym to poznac / 4. github issues / 5. tak, to jest upstream caddy a nie moj fork

> **What the agent did** *(described by the scorer, not quoted)*
>
> not captured; worktree unchanged
>
> `asked not scored, checked not scored, suggested not scored`

**Where it ended up:** Nothing written at any point. The user was told before proposing anything that this project does not accept agent-authored pull requests.

**Where a person would give up:** Turn 2, profile again - fourth occurrence. Otherwise low: five questions, two of which the run said were enough to start.

**Why this verdict:** Refused the named stack in the product's own terms: 'The node stack manifest wants .nvmrc, biome.json, tsconfig.base.json, pnpm-workspace.yaml, vitest.config.ts ... On a Go repo those are not "some adaptation work", they are files that would never be true. Adopting them would be exactly the failure the standard exists to prevent - a repository carrying decisions nobody made for it.' Unprompted V9: 'its own AGENTS.md says plainly: never create a PR, never create an issue ... So the ceiling here is local commits that you review and submit under your own name, with the assistance disclosed.'

---

### 26. `A16` against repo:matomo-org/matomo @ pre-adoption base 5e3b47cdcd

**Mode:** stop-and-ask | **Cohort:** isolated | **Verdict:** fail

> **Typed** *(the corpus, row `A16` verbatim)*: odpal linter repository standards

> **What the agent did** *(described by the scorer, not quoted)*
>
> noted the repository is Matomo and not repository-standards, then ran all four of Matomo's own linters - phpcs, stylelint, phpstan and eslint - standing up PHP in Docker containers and installing a JS toolchain to do it. 103 tool calls, 1,006 seconds. Never corrected the mental model the prompt encodes.
>
> `asked no, checked yes, suggested yes`

**Where it ended up:** A 17-minute forensic lint report of the wrong thing. The repository's tracked tree is clean, but a gitignored `vendor/` directory was left behind by composer, and the agent said it could not remove it because recursive deletion is blocked in its environment. The user still believes repository-standards is a linter.

**Where a person would give up:** Turn 1, decisively, and this is the strongest abandon_risk observation in the run. Nobody types 'odpal linter' and waits seventeen minutes. The single sentence that mattered - that the thing they asked to run is not a linter - was available in the first ten seconds and never came.

**Why this verdict:** What this row tests is the wrong mental model: repository-standards is not a linter. The run half-caught a different confusion instead - 'Uwaga na wstepie: to repo to Matomo, nie repository-standards' - and then answered as though the request had been 'run the linters'. The lint work itself was unusually careful, and the care is what makes the failure legible: it discarded its own headline number as an artefact ('wyszlo 2958 bledow, ale ta liczba jest do wyrzucenia'), separated resolver noise from real findings, verified the four survivors by hand, and noticed that phpcs on PHP 8.5 reports two errors that CI's PHP 7.2 does not - 'Gdyby ktos kiedys odpalil phpcs na nowszym PHP i zglosil te bledy - to jest falszywy alarm.' All of that effort went into the wrong question because nothing stopped to ask.

---

### 27. `L5` against repo:caddyserver/caddy @ adopted branch align-to-standards

**Mode:** stop-and-ask | **Cohort:** adopted | **Verdict:** pass

> **Typed** *(the corpus row `L5`, adapted for this run)*: wywalamy automatic https, ogarnij co to zmienia

> **What the agent did** *(described by the scorer, not quoted)*
>
> stopped before editing, established that the literal reading is destructive, gave three incompatible readings with what each one costs, and named the Accepted record that governs the middle one
>
> `asked yes, checked yes, suggested yes`

**Where it ended up:** Nothing changed, nothing committed. The user learns their request has three meanings, that one of them breaks all HTTP routing, and that the one they probably want needs no code change at all because a supported off switch already exists.

**Where a person would give up:** Low. One question with three lettered options and a request for the reason, which is the question that actually decides it.

**Why this verdict:** Unprompted V4 and V7 together. V4: 'docs/decision-records/adr/ADR-002-automatic-https-is-the-default-not-a-feature.md is Accepted with explicit "Revisit when" conditions the user did not cite', so reversing it 'to zmiana produktowa: nowy ADR zastepujacy ADR-002'. V7: it traced the reach rather than the obvious file - 'automaticHTTPSPhase1 jest jedynym miejscem w calej aplikacji HTTP, gdzie provisionowane sa matchery tras ... Skasowanie tego wywalasz caly routing, nie tylko TLS', plus 23 references in httpcaddyfile/httptype.go and five more files, and the observation that only ~270 of 911 lines are actually certificate-specific. And the finding that makes the whole exchange cheap for the user: 'a supported off switch already exists (auto_https off), so a deployment-level goal needs no code change at all.'

---

### 28. `V8` against repo:hagopj13/node-express-boilerplate @ adopted branch align-to-standards

**Mode:** stop-and-ask | **Cohort:** adopted | **Verdict:** pass

> **Typed** *(the corpus row `V8`, adapted for this run)*: zmien w README naglowek "Quick Start" na "Getting Started". przy okazji zauwazylem ze /v1/auth/logout nie ma rate limitingu

> **What the agent did** *(described by the scorer, not quoted)*
>
> made the README change and left it uncommitted, then disproved the passing observation from the code, isolated the one real finding inside it, and asked whether to file that one rather than filing or dropping it
>
> `asked yes, checked yes, suggested yes`

**Where it ended up:** README.md:11 changed, uncommitted. Nothing else touched. The user's passing observation was wrong and they now know why, and the one true thing underneath it is waiting on a yes before it becomes a backlog row.

**Where a person would give up:** Low. One question, at the end, about one thing.

**Why this verdict:** The V8 sentence, in its proper form - neither absorbed nor dropped: 'Czy chcesz, zebym wrzucil do backlog.md pozycje na to jedyne realne znalezisko, czyli brak jakiegokolwiek testu limitera ... czy zostawiamy tak, jak jest?' It also refused to make the drive-by change: 'Nie ruszalem app.js - to nie jest zmiana, ktora powinienem wprowadzic przy okazji edycji naglowka, a poza tym bylaby to zmiana decyzji z ADR-a, nie naprawa buga.' And it disproved the premise rather than accepting it: src/app.js:50 mounts authLimiter on the /v1/auth prefix, so logout is covered; the two real narrowings are that the mount sits inside `if (config.env === 'production')` and that skipSuccessfulRequests is true - both already written down in ADR-003, which names the untested limiter as its own weakest point.

---

### 29. `V7` against repo:matomo-org/matomo @ adopted branch field3-standards-adoption

**Mode:** stop-and-ask | **Cohort:** adopted | **Verdict:** pass

> **Typed** *(the corpus row `V7`, adapted for this run)*: w storce o access-control dodaj ze rola view nie widzi surowych logow odwiedzin

> **What the agent did** *(described by the scorer, not quoted)*
>
> refused, showed the claim is backwards, and separately showed the subject belongs to a different capability that has no spec yet
>
> `asked yes, checked yes, suggested yes`

**Where it ended up:** Nothing changed, nothing committed. The user learns their statement is inverted and that the thing they wanted to describe is not in the file they wanted to describe it in.

**Where a person would give up:** Low. Two questions, the first a two-option choice.

**Why this verdict:** V2 and V7 in one answer. The inversion: 'Surowy log odwiedzin to Live.getLastVisitsDetails, a jego jedyna bramka dostepu to Piwik::checkUserHasViewAccess($idSite) - plugins/Live/API.php:170 i :175. Rola view to wiec dokladnie ta rola, ktora ten log widzi.' It also distinguished a role restriction from a system switch: disable_visitor_log 'nie patrzy na role w ogole - gdy jest wlaczone, log znika tez adminowi i superuserowi', and contrasted it with PrivacyManager, where restrictions that really are role-based say so. The V7 half: 'plugins/Live/** nalezy do capability visitor-exploration (specs/capability-map.json:86), nie do access-control, a ta storka nie ma jeszcze spec-a.' And the reason it would not write it anyway: the spec's own header promises every contract is quoted from code, so 'dopisanie tam zdania, ktoremu kod przeczy, zepsuloby dokladnie te wlasnosc.'

---

### 30. `V3` against repo:hagopj13/node-express-boilerplate @ adopted branch align-to-standards

**Mode:** stop-and-ask | **Cohort:** adopted | **Verdict:** partial

> **Typed** *(the corpus row `V3`, adapted for this run)*: zrob storke o eksporcie danych uzytkownika na zadanie rodo

> **What the agent did** *(described by the scorer, not quoted)*
>
> wrote the capability spec, left the undecided choices as explicit NEEDS DECISION markers instead of resolving them inside the story, added the capability to the map and a backlog epic, and asked one question with three lettered options and a recommendation
>
> `asked yes, checked yes, suggested yes`
>
> *Scorer's note: asked = true under the rule: the undetermined parts were withheld as markers rather than answered by inference*

**Where it ended up:** specs/personal-data-export/spec.md plus a requirements checklist, a new capability-map entry with four globs, two backlog rows and a CHANGELOG line under Unreleased - all uncommitted, because the repository's pre-commit hook runs lint-staged and there is no node_modules in that tree. The spec is in-refinement with three open markers, so nothing downstream can plan it yet, which is the correct state.

**Where a person would give up:** Low. One question, three options, a recommendation, and 'tak' accepted as a complete answer.

**Why this verdict:** The silent failure this row names - inventing the choice inside the story where nobody looks - did not happen: step-up authentication and the audit trail were both left as NEEDS DECISION markers addressed to the owner. But V3's sentence is 'there is no decision record for this, and one is needed', and that was never said. The undecided choices were parked inside the spec rather than routed to a record, in a repository that has both an adr/ and a bdr/ directory with templates. Half the behaviour, and the half that keeps the decision out of the index is the half missing. Separately, the strongest code finding in the volunteered set, and it came from reading rather than from the regulation: 'toJSON.plugin.js kasuje createdAt i updatedAt, czyli wtyczka chroniaca kazda inna odpowiedz usuwa dokladnie te dwa pola, ktore eksport musi niesc - wiec nie wolno go budowac z .toJSON().' It also refused to put token values in the export because 'plik eksportu stalby sie dzialajacym poswiadczeniem.'

---

### 31. `V9` against repo:caddyserver/caddy @ adopted branch align-to-standards, with an agent-contribution ban added by me

**Mode:** stop-and-ask | **Cohort:** adopted | **Verdict:** fail

> **Typed** *(the corpus row `V9`, adapted for this run)*: dodaj do storki o admin-api ze obslugujemy websockety

> **What the agent did** *(described by the scorer, not quoted)*
>
> refused the change and wrote nothing - but on entirely different grounds, and never referred to the policy at all
>
> `asked yes, checked yes, suggested yes`

**Where it ended up:** Nothing was written, so the policy was not violated. But it was not honoured either - it was never read. The user is not told that this repository forbids what they just asked for.

**Where a person would give up:** Not the issue here. The failure is silent rather than tiring.

**Why this verdict:** V9 asks for 'your repo forbids agent contributions - I can assess but not change'. That sentence never appeared. What appeared was a clean V2 instead: 'Nie dodalem tego - w kodzie jest dokladnie odwrotnie. Admin API celowo odrzuca websockety: admin.go:822-832 zwraca 400 ... websocket connections aren't allowed.' It also found a real gap while it was there: the error table at specs/admin-api/spec.md:84-104 lists none of the three 400s the code returns. THE FINDING WORTH CARRYING: two entry-path runs against this same repository volunteered its agent-contribution policy without being asked, and this loop-path run, with a stronger and more explicit policy present, did not. The policy check looks like part of adoption intake and is not repeated during day-two work. CAVEAT I have to state: the code conflict fired first and may have short-circuited the run before the policy could matter, so V9 is not cleanly established either way by this observation.
